package main.daythree;

import java.util.Scanner;

public class EndTask {
    public static void main(String[] args) {
        // Create a scanner object to get input from the user
        Scanner scanner = new Scanner(System.in);

        //Ask the user for some input -> System.out.print("");

        //listen for the user input -> scanner.nextLine(); It will be string please define it

        //Ask the user some number input -> scanner.nextInt(); It will be int please define it

        //Do return the user response with both inputs in a sentence -> System.out.println("Your response: " + response);



        // Close the scanner
        scanner.close();
    }
}
