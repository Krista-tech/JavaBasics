package main.extatasksfirst;

import java.util.Random;
import java.util.Scanner;

public class ExtraTaskFour {
    /**
     * Story:
     * The user has a shopping list of five items. Their task is to enter items, and the program will check
     * if each item is already on their list. If an item is found, it confirms this, and if not,
     * it notifies them to add it.
     */

    //Requirements:
    //
    // Create a String array to store a shopping list with five predefined items.
    //Ask the user to enter items they have bought.
    //For each item entered, check if it’s on the shopping list.
    //If it’s on the list, display a message confirming it’s found. If not, display a reminder to add the item to the list.
    //Allow the user to check items one by one until they’ve verified all items.
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);


        // Define a shopping list with 5 items
        //print welcome message
        //print shopping list

        // Loop to check each item the user enters

        //for (int i = 0; i < shoppingList.length; i++) {
        // Ask the user to enter an item
        // Check if the item is on the shopping list
        // If the item is on the list, print a confirmation message
        // If the item is not on the list, print a reminder to add it





        // Close the scanner

    }
}