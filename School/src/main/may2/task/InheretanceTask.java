package main.may2.task;

// Bonus task, add description of what each class does in kdoc
public class InheretanceTask {
    //Create new classes instead of writing in this class

    // TASK 1: Create a superclass called `Vehicle`
    // Add the field: String brand
    // Add a method: startEngine()
    // Print: "[brand] engine started!"

    // TASK 2: Create a subclass called `Car` that extends Vehicle
    // Add a field: String model
    // Add a method: drive()
    // Print: "[brand] [model] is driving!"

    // TASK 3: BONUS — Add another subclass: `Bicycle`
    // Add a method: ringBell()
    // Print: "[brand] is ringing the bell! 🚲"

    // TASK 4: BONUS — Inside Bicycle, add a field: int gearCount
    // Add a method: showGears()
    // Print: "[brand] has [gearCount] gears."

    // TASK 5: SUPER BONUS — Try using all of these in the main method!
}
