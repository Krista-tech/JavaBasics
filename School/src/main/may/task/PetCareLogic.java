package main.may.task;


//Bonus task, add description of what this class does in kdoc
public class PetCareLogic {

    // TASK 1: Create a class called `PetCare`
    // Add these private fields:
    // - String petName
    // - int happinessLevel

    // TASK 2: Add a public method setPetName(String name)
    // and a getPetName() method to return it

    // TASK 3: Add a method feedPet(int foodAmount)
    // If foodAmount > 0, increase happinessLevel by foodAmount
    // Else, print: "You can't feed with a negative amount!"

    // TASK 4: Add a method checkHappiness()
    // Return the current happiness level

    // TASK 5: BONUS — Add a method playWithPet(int minutes)
    // Each minute should add 2 happiness points
    // If minutes < 0, print a warning

    // TASK 6: BONUS — Add a method getMood()
    // If happinessLevel > 20 → return "Super happy!"
    // If 10–20 → return "Pretty happy"
    // If below 10 → return "Needs love 🐾"
}
