package main.may3.task;



public class PolymorphismLogicTask {

    // TASK 1: Create a superclass called `Instrument`
    // Add a field: String name
    // Add a method: play()
    // Print: "[name] is making music."

    // TASK 2: Create two subclasses: `Guitar` and `Drum`
    // Override the play() method in both classes
    // Guitar: "[name] goes strum strum!"
    // Drum: "[name] goes boom boom!"

    // TASK 3: BONUS — Add another subclass: `Flute`
    // Override play() to print: "[name] plays a soft melody..."

    // TASK 4: BONUS — Create a method in Main to playAll(Instrument[] instruments)
    // Loop through all and call play() — each will sound different!
}
