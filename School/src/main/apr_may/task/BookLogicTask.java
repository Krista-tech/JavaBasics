package main.apr_may.task;


public class BookLogicTask {

    // TASK 1: Create a class named `Book`
    // Add the following fields:
    // - String title
    // - String author
    // - int year
    // - boolean isAvailable

    // TASK 2: Add a method `displayInfo()`
    // Print: "Title: [title], Author: [author], Year: [year], Available: [true/false]"

    // TASK 3: Add a method `markAsBorrowed()`
    // It should set isAvailable to false and print: "[title] is now marked as borrowed."

    // TASK 4: Add a method `markAsReturned()`
    // It should set isAvailable to true and print: "[title] is now available."

    // TASK 5: BONUS — Add a method `getAge(int currentYear)`
    // It should return the age of the book (e.g., 2025 - year)
    // In Main, display: "'[title]' is [x] years old."

    // TASK 6: BONUS — Add a method `isWrittenBy(String searchAuthor)`
    // Return true if the author's name matches the input (ignore case)
}
